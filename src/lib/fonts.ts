import { Inter, Varela_Round } from "next/font/google";

export const open = Inter({
  subsets: ["latin"],
  variable: "--font-open",
  display: "swap",
});

export const varela = Varela_Round({
  subsets: ["latin"],
  weight: "400",
  variable: "--font-varela",
  display: "swap",
});
